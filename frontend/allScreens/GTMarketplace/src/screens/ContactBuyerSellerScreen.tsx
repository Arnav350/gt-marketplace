import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ContactBuyerSellerScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Contact Buyer/Seller Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default ContactBuyerSellerScreen;
